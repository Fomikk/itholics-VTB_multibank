# 🚀 Быстрый запуск FinGuru

## Шаг 1: Запустите Backend

Откройте **новый терминал PowerShell** и выполните:

```powershell
cd C:\itholics-VTB_multibank\itholics-VTB_multibank\backend

# Простой способ (автоматически создаст venv и установит зависимости):
.\start.bat

# Или вручную:
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```

**Проверьте:** Откройте http://localhost:8000/health в браузере
- Должно вернуть: `{"status":"ok"}`

## Шаг 2: Запустите Frontend

Откройте **еще один новый терминал PowerShell** и выполните:

```powershell
cd C:\itholics-VTB_multibank\itholics-VTB_multibank\frontend
npm run dev
```

**Проверьте:** Откройте http://localhost:5173 в браузере
- Должен открыться Dashboard

## ⚠️ Важно

1. **Backend должен быть запущен ПЕРЕД frontend**
2. Оба терминала должны оставаться открытыми
3. Если видите ошибку 500 - проверьте, что backend запущен на порту 8000

## 🔧 Если что-то не работает

### Backend не запускается:
- Проверьте, что Python 3.11+ установлен: `python --version`
- Убедитесь, что порт 8000 свободен
- Проверьте логи в терминале backend

### Frontend показывает ошибку:
- Убедитесь, что backend запущен: http://localhost:8000/health
- Проверьте консоль браузера (F12) для деталей ошибки
- Проверьте терминал frontend для ошибок

### Ошибка 500:
- Backend не запущен или упал
- Проверьте логи backend в терминале
- Убедитесь, что `.env` файл создан в `backend/`

