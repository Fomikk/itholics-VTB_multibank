# 🚀 Как запустить Backend

## Проблема: ECONNREFUSED

Ошибка `ECONNREFUSED` означает, что backend не запущен на порту 8000.

## Решение

### Шаг 1: Откройте новый терминал PowerShell

**Важно:** Не закрывайте терминал frontend! Откройте **НОВЫЙ** терминал.

### Шаг 2: Запустите backend

```powershell
# Перейдите в директорию backend
cd C:\itholics-VTB_multibank\itholics-VTB_multibank\backend

# Активируйте виртуальное окружение
venv\Scripts\activate

# Запустите сервер
uvicorn app.main:app --reload
```

### Шаг 3: Проверьте, что backend работает

Откройте в браузере: **http://localhost:8000/health**

Должно вернуть: `{"status":"ok"}`

### Шаг 4: Обновите frontend

Вернитесь к frontend (http://localhost:5173) и обновите страницу (F5).

## Ожидаемый результат

После запуска backend вы должны увидеть в терминале:

```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

## Если backend не запускается

Проверьте логи в терминале - там будет указана причина ошибки.

Возможные проблемы:
- Порт 8000 занят другим процессом
- Отсутствуют зависимости (запустите `pip install -r requirements.txt`)
- Ошибки в коде (проверьте логи)

## Важно

- **Backend должен быть запущен ПЕРЕД использованием frontend**
- Оба терминала (backend и frontend) должны оставаться открытыми
- Backend должен работать на **http://localhost:8000**

