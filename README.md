# FinGuru (itholics-VTB_multibank)

Мультибанковское PWA + API для агрегации счетов/транзакций и мини-игры с кешбеком.

## Цель
- Подключать банки через Open Banking API (в т.ч. ГОСТ-шлюз).
- Агрегировать счета/балансы/транзакции (ABank, SBank sandbox).
- Показать дашборд (баланс, динамика, категории).
- Дать мини-игру → активировать повышенный кешбек в категории (демо-логика).

## Архитектура (MVP)
- **backend/** FastAPI:
  - adapters/: ABank, SBank (работают по их OpenAPI).
  - services/: Aggregation, Cashback, Games.
  - api/: REST-роуты `/auth`, `/accounts`, `/transactions`, `/cashback`, `/game`.
  - core/: конфиги, клиенты HTTP, обработка ошибок, валидация.
- **web/** React (Vite):
  - Дашборд (Net Worth, графики, категории), список транзакций, экран мини-игры.
- **db:** PostgreSQL (пользователи, соединения с банками, кэш транзакций, игровые бонусы).
- **интеграции:** JWKS валидация токенов банков при межбанковых запросах.

## Банковские спецификации (sandbox)
- `openapi/abank.json` — ABank
- `openapi/sbank.json` — SBank (требует ручное одобрение согласий)
> Файлы добавлены как есть от организаторов.

## Нефункциональные
- Ошибки/ретраи, кэш последних успешных данных.
- Безопасность: `.env` секреты, не коммитить `.env*`.
- Стиль: `black`, `ruff`, `mypy` (backend); ESLint/Prettier (web).

## 🚀 Быстрый старт

### Локально (без Docker)

**1. Backend:**
```powershell
cd backend
venv\Scripts\activate
uvicorn app.main:app --reload
```

**2. Frontend (в новом терминале):**
```powershell
cd frontend
npm run dev
```

**Проверка:**
- Backend: http://localhost:8000/health
- Frontend: http://localhost:5173
- API Docs: http://localhost:8000/docs

### С Docker

```bash
docker compose -f docker-compose.yml -f docker-compose.dev.yml up
```

---

## 🔑 Режимы работы

### Демо-режим (без credentials)

**Система работает без credentials!** Если в `.env` нет `CLIENT_ID` и `CLIENT_SECRET`, система автоматически:
- Пропускает запросы к банкам
- Генерирует демо-данные для привязанных счетов
- Показывает реалистичные балансы, транзакции и аналитику

**Просто привяжите счета** через http://localhost:5173/connect и данные появятся автоматически!

### Реальный режим (с credentials)

Для получения реальных данных от банков добавьте credentials в `.env`:

```env
VBANK_CLIENT_ID=team268
VBANK_CLIENT_SECRET=ваш_секретный_ключ
ABANK_CLIENT_ID=team268
ABANK_CLIENT_SECRET=ваш_секретный_ключ
SBANK_CLIENT_ID=team268
SBANK_CLIENT_SECRET=ваш_секретный_ключ
```

**Важно:** После изменения `.env` перезапустите backend!

Credentials обычно предоставляют организаторы конкурса/хакатона. После добавления credentials система автоматически переключится на реальные данные.

---

## 🔗 Подключение банковских счетов

1. Откройте http://localhost:5173/connect
2. Выберите банк и введите номер счета
3. Данные автоматически появятся на Dashboard

### Типы согласий

**VBank и ABank** (автоматическое одобрение):
- Согласие создается и одобряется автоматически
- Данные доступны сразу после создания согласия

**SBank** (ручное одобрение):
- Согласие создается со статусом "pending"
- Необходимо зайти в SBank и одобрить запрос на доступ
- После одобрения данные станут доступны
- Система покажет демо-данные до одобрения согласия

---

## 🧪 Тестирование API

**Swagger UI:** http://localhost:8000/docs

**Примеры запросов:**
- `GET /api/analytics/summary?client_id=team268-1&period=30d`
- `GET /api/accounts/aggregate?client_id=team268-1`
- `POST /api/cashback/activate` (см. Swagger для примера тела)

---

## 📋 План получения данных из банков (3 шага)

Система следует стандартному плану межбанковского доступа:

### Шаг 1: Получение bank token
```
POST /auth/bank-token?client_id=team268&client_secret=...
→ { access_token: "eyJ...", expires_in: 86400 }
```
- Токен живет 24 часа
- Для каждого банка - свой токен
- Используется для всех межбанковых запросов

### Шаг 2: Создание согласия
```
POST /account-consents/request
Headers: Authorization: Bearer <bank_token>, X-Requesting-Bank: team268
Body: { permissions: [...], client_id: "team268-1", requesting_bank: "team268" }
→ { consent_id: "...", status: "approved" }  (VBank/ABank)
→ { request_id: "...", status: "pending" }   (SBank)
```
- **Важно:** `X-Requesting-Bank` = `team268` (без суффикса)
- Permissions: `ReadAccountsDetail`, `ReadBalances`, `ReadTransactions`
- VBank/ABank: автоматическое одобрение
- SBank: требует ручного одобрения клиентом

### Шаг 3: Получение данных
```
GET /accounts?client_id=team268-1
Headers: Authorization: Bearer <bank_token>, X-Requesting-Bank: team268, X-Consent-Id: <consent_id>
→ { accounts: [...] }
```
- Всегда указываем `X-Requesting-Bank: team268`
- Обязательно указываем `X-Consent-Id` для одобренных согласий
- Аналогично для `/balances` и `/transactions`

**Результат:** 3 запроса → данные готовы к агрегации!

